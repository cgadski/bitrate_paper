## experiments

Experiments that our graphed are based on are defined in`project/eta_sweep.py` and `project/k_sweep.py`.

Graphs are produced by `project/graphs/k_sweep.py` and `project/graphs/eta_sweep.py`.
